
  create or replace   view ALFA_PROJEKT.SILVER_SILVER.stg_traffic
  
   as (
    SELECT TRAFFIC_ID, EVENT_TIME::TIMESTAMP AS EVENT_TIMESTAMP, ITEM_IN_CART, PRODUCT_ID, ORDER_ID FROM ALFA_PROJEKT.RAW.FACT_TRAFFIC
  );

